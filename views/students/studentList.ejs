<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Attendance</title>
</head>
<body>
    <h1>Students Here:</h1>
    <ul>
        <li>
            Name: Present
        </li>
        <% studentsPresent.forEach((item) => { %>
            <li>
                <%= item.name %>
                : 
                <%= item.present %>
            </li>
        <% }); %>
    </ul>
</body>
</html>